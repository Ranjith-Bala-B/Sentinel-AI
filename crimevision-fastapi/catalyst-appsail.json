{
  "name": "sentinel-ai-backend",
  "stack": "python_3_11",
  "command": "sh -c 'python3 -m uvicorn main:app --host 0.0.0.0 --port ${X_CATALYST_PORT:-${PORT:-8080}}'",
  "build_path": "./",
  "scripts": {
    "build": "pip install -r requirements.txt"
  }
}
